import { Header, Form, Span, Button, Input } from './Searchbar.styled';

const Seachbar = () => {
  <Header>
    <Form>
      <Button type="submit">
        <Span></Span>
      </Button>
      <Input type="text" placeholder="Search" autocomplete="off" />
    </Form>
  </Header>;
};
export default Seachbar;
